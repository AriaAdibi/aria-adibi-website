{
  "name": "portfolio-workshop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist .vite",
    "clean:full": "rm -rf node_modules dist .vite package-lock.json"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  },
  "dependencies": {
    "@prismicio/client": "^7.3.1"
  }
}
